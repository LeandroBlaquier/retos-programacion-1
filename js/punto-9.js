// const cantidadDeDias = Number(prompt("Introduce una cantidad de dias"));

// function calculo(dias) {
//   const horas = dias * 24;
//   const min = dias * 1440;
//   const segundos = dias * 86400;
//   alert(
//     `${cantidadDeDias} dias tiene ${horas} horas, ${min} minutos y ${segundos} segundos`
//   );
// }
// calculo(cantidadDeDias);
